
import { html, svg } from "lit-html";

/**
 * @class Chart
 * @desc The underlying machinery that allows for the creation of a chart.
 */
class Chart {
    static defaultHeight = 240;
    static defaultWidth = 280;
    static defaultGap = 2;
    
    constructor() {
    
    }
}
